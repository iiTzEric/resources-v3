Big O Notation

Big O measures how work grows as input grows.

O(1) — constant, same work regardless of input size
O(n) — linear, work grows in proportion to input
O(n²) — quadratic, work grows by the square (nested loops)
O(log n) — logarithmic, work barely grows (halving each step)